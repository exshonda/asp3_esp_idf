# C6 evidence-12 — **`0x600af018` は «既に shim に在る»／app 側の重複を除去する**

日付: 2026-07-17 ／ branch: `claude/c5-espidf-supply-migration`
DUT: **ESP32-C6 `14:C1:9F:E0:5A:9C`＝board C**（hub `1-6` port2）
前段: `evidence-c6-11`（`--wrap` 計装を既定から除去／app 側の live 書込みを «触らず» 報告）

★**認証情報はビルド注入のみ。本ファイルには書かない。**

---

## 1. ★★前提の訂正 — **「shim へ恒久移設する」は «既に済んでいる»**

**依頼は「`0x600af018` を app から shim（`esp_wifi_adapter.c`）へ恒久移設せよ」だったが、
実測の結果 «shim には既に在る»**：

| 場所 | 関数 | 内容 | live か |
|---|---|---|---|
| `wifi/esp_wifi_adapter.c:589` | **`phy_enable_wrapper()`**（定義 581） | `*(volatile uint32_t *)0x600af018U = 0x7U;`「★根本原因修正（追記10）」 | **★live**（ELF に実在・osi テーブル登録） |
| `wifi/esp_wifi_adapter.c:746` | **`wifi_clock_enable_wrapper()`**（定義 689） | 同上「★根本原因修正（追記10）：`wifi_module_enable()` 後も…」 | **★live** |

⇒ **`apps/wifi_scan/wifi_scan.c:807`／`:830` の書込みは «重複»**（shim が既に同じ値を書いている）。
⇒ **∴ «移設» は不要**。**やるべきは «app 側の重複と診断の整理» だけ**。

★**この訂正は «依頼の前提» を覆すので、実機の前に記録する**（親指示「私が書いた事実も裏を取れ」）。

### 1.1 ★予測が «依頼の想定» と食い違う（先に宣言する）

依頼は **対照2「app 診断を外すと 0 AP に戻るはず＝«診断が修正だった» の実証」** を求めているが、
**§1 が真なら 0 AP には «戻らない»**（shim が既に書いているため）。

| # | 測定 | **依頼の想定** | **私の予測** | 確度 | 根拠 |
|---|---|---|---|---|---|
| **C2** | app 診断を外して 真cold `wifi_scan` | **0 AP に戻る** | **★13 AP 相当のまま（回帰しない）** | **85%** | §1＝shim の `phy_enable_wrapper`／`wifi_clock_enable_wrapper` が同じ値を書く＝app の書込みは重複 |
| **C3** | 移設後（＝app 整理後）真cold `wifi_scan` | 13 APs | **13 AP 相当** | 85% | C2 と同じ（«移設» は no-op なので C2＝C3） |
| **W1** | 真cold `wifi_dhcp` | 維持 | **`IP acquired`＋ping 失敗 0** | 80% | app(`wifi_dhcp`) は元から診断非参照 |

★**含意の自問**：
- 「C2 が «13 AP» ⇒ app の書込みは無用だった」＝**健全**（重複の実証）。
- 「C2 が «13 AP» ⇒ `0x600af018` の設定自体が不要」＝**★健全でない**。
  **shim が書いているから通っている**のであって、**両方外したら 0 AP に戻る可能性は残る**
  （＝追記10 の主張は shim 側で生きている）。**shim の書込みは外さない。**
- 「C2 が **0 AP** ⇒ §1 が誤り／shim の書込みが効いていない」＝**健全**＝そのときは
  **shim の2箇所が本当に呼ばれているかを JTAG/計装で測る**（＝止めて報告）。

## 2. ★ビット同定（依頼 (b)）について — **今回は «測らない»。理由を書く**

依頼は「`0x7` のうちどのビットが要るかを外して測れ」。**しかし §1 より、いま `0x7` を書いているのは
«shim»** であり、**app 側は重複**。∴ **app を外してもビットは変わらない（shim が 0x7 を書く）**
＝**app 経由ではビット同定はできない**。**ビット同定は «shim の値を変える» 実験**になり、
**それは «診断の整理» ではなく «恒久修正の変更»**＝**別の実験**（＝範囲を広げると何が効いたか
分からなくなる、という親指示自身の理由がここにも当てはまる）。

★**ただし «測るべき» 材料は見つけたので記録する（§5）**：**shim は `= 0x7`（代入）で
bit3 以上を潰している**が、**BT（`bt_shim.c`）は `|= 0x1`（read-modify-write）で温存する**。
**memory §12 は «LPCON 0x600af018 bit3 = LP_TIMER_EN は BT 正当» と実測記録**している
⇒ **`= 0x7` は bit3 を消す＝WiFi/BT 同時運用時のハザード**（現状は排他なので顕在化しない）。

---

## 3. 実機測定

（実機実施後に記入）

---

## 4. 非回帰

（実機実施後に記入）

---

## 5. `g_phyFuns` パッチの調査所見（触っていない・依頼 (e)）

（記入）
