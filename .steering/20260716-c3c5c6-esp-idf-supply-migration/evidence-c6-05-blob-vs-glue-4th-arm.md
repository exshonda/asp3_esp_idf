# C6 evidence-05 — **4アーム目：blob か グルーか の帰属を決める**

日付: 2026-07-17 ／ branch: `claude/c5-espidf-supply-migration` ／ 前段 commit: `097b4bd`
DUT: **ESP32-C6 `14:C1:9F:E0:5A:9C`**（hub `1-6` port2）
前段: `evidence-c6-04` ＝ **真cold(POR) の phy_init ハングを根治**（ROM は CPU を XTAL@40MHz で渡す）

---

## 1. 目的（1行）

**§13 が «blob と グルーを同時に替えた» まま「非収束は blob の版が原因」と記録した交絡を、
«blob をバイト固定してグルーだけ替える» 4アーム目で解く。**

## 2. 前提が変わった（なぜ今できるのか）

`evidence-c6-02` は4アーム目を実機で回して **BLOCKED**（個体が何を焼いても通らなかった）。
**その原因は evidence-c6-04 が根治した «真cold init バグ»** だった。**いま cold は通る**
（BT D-1 `0xb1d00008` 独立2/2・WiFi 16-20 AP）。∴ **4アーム目は実行可能になった**。

---

## 3. 着手前に**自分で実測**した事実（引き継ぎを鵜呑みにしない）

### 3.1 ★blob のバイト同一性 — **実際にリンクされるパスで** `cmp` 検証

★私は最初 `find | head -1` で md5 を採り、**v6.1 の libphy を別ファイルと取り違えた**
（`cf9ff744` と出たが、**実際にリンクされるのは `3fea0708`**）。
⇒ **`build.ninja` の `-L` から実パスを取り直して `cmp` で確認した**（以下が正）。

| lib | **v554**（`esp-idf/`＝v5.5.4 タグ） | **hal**（`hal/`） | **v61**（`~/tools/esp-idf-v6.1`） |
|---|---|---|---|
| `libble_app.a` | `75db98e5…` | **`75db98e5…`** | `c28653df…` |
| `libphy.a` | `cb429107…` | **`cb429107…`** | `3fea0708…` |

**`cmp -s` の結果**：**`v554 ≡ hal` は `libble_app.a`・`libphy.a` **とも** バイト同一**（★決定的）。
`v554 ≠ v61`（両 lib とも）。
★**∴ v6.1 swap は libble_app.a «と» libphy.a の «両方» を替えている**（§13 の「blob」は2本）。

### 3.2 ★グルーの型 — 実測（`bt.c` を grep）

| arm | `bt.c` 実パス | `esp_os_*` | `npl_os_*` | `esp_intr_alloc` | md5(先頭12) |
|---|---|---|---|---|---|
| **hal** | `hal/components/bt/controller/esp32c6/bt.c` | **5** | **9** | 5 | `ad50107280b6` |
| **v554** | `esp-idf/components/bt/controller/esp32c6/bt.c` | **0** | **0** | 7 | `9a605928ef72` |
| **v61** | `~/tools/esp-idf-v6.1/…/esp32c6/bt.c` | **0** | **0** | 7 | `1ce2b05e973e` |

⇒ **hal «だけ» が `esp_os_*`/`npl_os_*` 型。v554 と v61 はともに C3型**＝引き継ぎ追認。
**ELF 実測でも** `esp_os_` シンボルは hal=4個／v554=0／v61=0。

### 3.3 ★3アームとも **ビルド通過**（＝対照は取れる）

★`evidence-c6-01` は「**§20 が実体を `esp_bt_idf61.cmake` にしか追加せず hal 経路は
2026-07-15 以降ビルド不可**」と報告したが、**それは evidence-c6-01 自身が修正済み**
（ガードを `TOPPERS_ESP32C6_BT_PMU_INIT` へ変更＝実体を積む cmake だけが定義）。
⇒ **hal 対照は «無理に作る» 必要なく普通にビルドできる**（実測）。

**tripwire（全アーム実測）**：

| arm | `bt.c` object の出所 | blob `-L` の出所 | ELF の `esp_os_` 数 | **cold 修正リンク** | **D-1 トレース** |
|---|---|---|---|---|---|
| v61 | v6.1 tree | v6.1 | 0 | **1** | **有効** |
| **v554** | **`esp-idf/`（v5.5.4 タグ）** | **`esp-idf/`** | **0** | **1** | **有効** |
| hal | `hal/` | `hal/` | **4** | **1** | **有効** |

★**3アームとも evidence-04 の cold 修正が入っている**（`ESP32C6_COLD_CPU_PLL` は
`(WIFI OR BT)` ゲートなので hal 経路にも効く）＝**cold 修正は «定数»**。
★**3アームとも同一アプリ `bt_smoke_c6` ＋ `TOPPERS_C6_BT_D1_TRACE`**
＝**同一マーカで判定**（evidence-04 末尾の «トレース無しビルドで STORE0=0 を
ハングと誤読しかける» 罠を回避）。

### 3.4 ★★「単一変数」なのはどのペアか（**ここを厳密に**）

| ペア | 変わるもの | 変わらないもの | **単一変数か** |
|---|---|---|---|
| **hal ↔ v554** | **グルーのみ**（`bt.c` 型＋シム `bt_shim.c`→`bt_shim_idf61.c`） | **blob 2本ともバイト同一**（§3.1 `cmp` 実証） | **★YES＝これが決定的** |
| v554 ↔ v61 | **blob 2本**＋**グルーの微差**（`bt.c` md5 が違う＝同じ «C3型» だが同一ではない） | グルーの «型» | **★NO（2変数）** |

⇒ **本ラウンドの決定力は «hal ↔ v554» に在る**（blob をバイト固定してグルーだけ替える）。
**v554 ↔ v61 は補助**であり、**単独で blob を有罪にはできない**（グルーの微差が残る）。
★これは**§13 の交絡（blob×グルー同時）を «完全には» 解かない**——
**解けるのは「blob X が無罪か否か」まで**。**そこまでで止める。**

---

## 4. ★★実機前に固定する予測（★ここから下は実機を1回も触らずに書いた）

### 4.1 予測

| # | arm | 条件 | **予測** | 確度 | 根拠 |
|---|---|---|---|---|---|
| **Q1** | **v61** | **warm** | **PASS**（`0xb1d00008`） | **95%** | evidence-04 #13 で実測済（同一構成） |
| **Q2** | **v61** | **真cold** | **PASS** | **95%** | evidence-04 で独立2/2 実測済 |
| **Q3** | **hal** | **warm** | **HANG** | **70%** | §10-12 の warm 観測（`register_chipv7_phy` synth-lock）。**★70% にする理由**＝evidence-04 の cold 修正が **ICG preinit＋regi2c マスタを «main_task より遥かに早く»** 実行するようになった＝**hal のハングごと直る可能性が残る**（＝それはそれで大発見） |
| **Q4** | **hal** | **真cold** | **HANG** | **75%** | 同上 |
| **Q5** | **v554（★4アーム目）** | **warm** | **50%/50%** | — | **本ラウンドの問い。事前に賭けない** |
| **Q6** | **v554（★4アーム目）** | **真cold** | **50%/50%** | — | 同上 |

★**Q5/Q6 を «50%» と書くのは逃げではない**：材料が両方向にある。
- **PASS 寄り**：**libphy `cb429107` は «真cold で WiFi を通している»**（evidence-04＝
  `esp-idf` 供給の `wifi_scan` が 16-20 AP）＝**この libphy バイト列は収束できる**。
  §11 が見つけた «BT 経路だけ欠けていたクロック設定» はグルー側の話。
- **HANG 寄り**：§12 が「同一 hal libphy・同一入力・同一状態で WiFi 収束／BT 非収束
  ＝発散点は `register_chipv7_phy` 内部」と実測している＝**blob 内部の BT サブパス**の疑い。

### 4.2 ★含意表 — **「反証条件も仮説である」を自問した結果**

#### (A) **v554 が PASS**（かつ hal が HANG）した場合

| | 主張 | 健全か |
|---|---|---|
| **A-1** | **blob 2本（`75db98e5`＋`cb429107`）は «この構成で D-1 に到達できる»** | **★健全**（実測そのもの） |
| **A-2** | **「非収束は blob の版が原因」は反証される** | **★健全**＝hal と v554 は **blob がバイト同一**（§3.1 `cmp`）。**同じバイト列が一方で通り一方で落ちる ⇒ blob «だけ» では説明できない** |
| **A-3** | **犯人は hal の «グルー»**（`bt.c` の `esp_os_*`/`npl_os_*` 型＋シム） | **★健全**（hal↔v554 の唯一の差） |
| **A-4** | 「`esp_os_*` ドリフトが具体的な機序だ」 | **★健全でない＝断定禁止**。グルーは `bt.c`＋シムの**塊**であり、**どの1行が効いたかは未分離** |
| **A-5** | **∴ C6 も v5.5.4 統一«可能»＝据置き撤回** | **★健全**（供給移行の可否は «v554 で D-1 が出るか» そのもの） |

#### (B) **v554 が HANG**（かつ v61 が PASS）した場合

| | 主張 | 健全か |
|---|---|---|
| **B-1** | **blob `75db98e5`/`cb429107` は «2種類のグルーの両方で» 落ちる** | **★健全**（hal も v554 も落ちる＝実測） |
| **B-2** | 「**blob の版が原因**＝据置き «実証済み»」 | **★健全でない＝ここが罠**。v554↔v61 は **blob «と» グルー微差の2変数**（§3.4）。∴ 正確には「**C3型グルーに替えても blob X では通らない ⇒ blob X が «必要条件を満たさない» 疑いが強い**」まで。**「blob が原因」と言い切るには v6.1 グルー×blob X の5アーム目が要る**（＝本ラウンドの射程外） |
| **B-3** | **∴ 据置き（v6.1 必須）を «実務上» 維持** | **★健全**（実務判断としては十分＝v554 で D-1 が出ない以上 v5.5.4 統一はできない） |

#### (C) **hal が PASS してしまった場合**（Q3/Q4 が外れ）

| | 主張 | 健全か |
|---|---|---|
| **C-1** | **evidence-04 の cold 修正が §10-12 の hal ハングごと直した** | **★健全な仮説だが要検証**＝§10-12 は **warm** 観測。**同一条件（warm）で hal が通るなら «修正が効いた»**（cold 修正の ICG/regi2c 前倒しが warm にも効く経路は実在する） |
| **C-2** | 「**4アーム目の意味が変わる**」 | **★健全**＝hal も v554 も通るなら **blob もグルーも無罪＝真犯人は evidence-04 が直したもの**＝**§13 の «v6.1 必須» は丸ごと不要だった**ことになる（＝**最大の発見**） |

★**∴ «hal 対照» を省いてはいけない**（親指示どおり）。**«v554 が動いた» の意味は
«hal が同一条件で落ちる» ことに完全に依存する。**

### 4.3 ★条件の明示（**本セッションで実際に踏みかけた事故**）

**§10-13 の観測はすべて warm**（memory「真cold未検証（全RTS）」）。
∴ **結論には «warm で言っているのか cold で言っているのか» を必ず書く**。
**両条件で測る**。（evidence-04 §4.4 で私は「cold と warm を取り違えて相手の観測を
誤って反証しかけた」＝自分で捕まえた。）

### 4.4 測定の作法

- **真cold の証明＝2重**：`uhubctl -l 1-6 -p 2 -a off` ＋ **by-id 読み戻し 0**、
  **かつ** センチネル `0xCAFE5A9C`→電源断→**`0x00000000`**（＝マーカ非stale も同時証明）。
- **cold 中は UART を開かない**（判定は LP_AON 直読み）。**warm の `rst:` 採取は
  «ブートが始まる前に1回だけ open して保持»**。
- **`grep -a` 必須**。**`rst:` を毎回記録**。
- ★**LP_AON は `STORE0`(`0x600B1000`)＋`STORE7`(`0x600B101C`) のみ使う**
  （`bt_smoke_c6` の既存 2 本）。**`STORE4`(`0x600B1010`)＝`RTC_XTAL_FREQ_REG`・
  `STORE1`(`0x600B1004`)＝`RTC_SLOW_CLK_CAL_REG` には触らない**（evidence-04 の申し送り）。
- **`STORE0=0` を «ハング» と読む前にトレース有効を確認**（§3.3 で3アームとも有効を実測済）。

---

## 5. 実機測定（2026-07-17）— **★3アームとも D-1 到達**

### 5.0 結論（先に4行）

1. **★4アーム目（v554＝C3型グルー × hal同一 blob）は warm・真cold の «両方» で D-1 到達**
   ⇒ **blob `75db98e5`/`cb429107` は «D-1 に到達できない blob» ではない＝blob 無罪**。
   ⇒ `evidence-c6-01 §7.1` の**捨て身の予測（「ハングする側に賭ける」＝blob 起因）は反証**。
2. **★しかし «hal 対照» も両条件で D-1 到達**（＝**§10-12 の «hal は warm でハング» が
   この個体で再現しない**）⇒ **グルーも無罪**。
   ⇒ **帰属は «blob でもグルーでもない»**。**§13 の «非収束は blob の版» という記録は、
   この個体では «現象そのものが再現しない»**。
3. **★逆方向対照で «私の cold 修正が hal を直したのではない» ことを確定**：
   **hal は `ESP32C6_COLD_CPU_PLL=OFF` でも warm で D-1**（＝§4.2(C-2) の
   「真犯人は evidence-04 が直したもの」は **warm については反証**）。
   **真cold だけは修正が要る**（evidence-04 どおり＝cold バグは供給に依らず普遍）。
4. **★実務判断＝据置き（v6.1必須）は «撤回できる»**：**v554（submodule 供給）が
   warm・真cold とも D-1**。**ただし v554 の実機実績は D-1 まで**（v6.1 は D-2b/D-2c まで）
   ＝**既定を替えるなら D-2b の確認を先に**（§6.3）。

### 5.1 ★測定マトリクス（**全て本個体 `14:C1:9F:E0:5A:9C`・本セッション・同一アプリ `bt_smoke_c6`＋同一 D-1 マーカ**）

真cold の証明＝毎回2重（by-id 読み戻し 0 ＋ センチネル `0xCAFE5A9C`→`0x00000000`）。
warm は `rst:` を毎回採取＝**全て `rst:0x15 (USB_UART_HPSYS)`**。

| # | arm | グルー | blob | **warm** | **真cold** |
|---|---|---|---|---|---|
| 1 | **hal**（対照） | **hal型**（`esp_os_*`=5/`npl_os_*`=9・ELF 4 syms） | `75db98e5`/`cb429107` | **`0xb1d00008` D-1**（console `Phase D-1 milestone reached`） | **`0xb1d00008` D-1** |
| 2 | **hal ＋ `COLD_CPU_PLL=OFF`**（★逆方向対照） | 同上 | 同上 | **`0xb1d00008` D-1** | **`0xb1d00005` ハング** |
| **3** | **★v554（4アーム目）** | **C3型**（`esp_os_*`=0・ELF 0 syms） | **`75db98e5`/`cb429107`＝#1 と «バイト同一»** | **`0xb1d00008` D-1** | **`0xb1d00008` D-1** |
| 4 | **v61**（対照） | C3型 | `c28653df`/`3fea0708` | **`0xb1d00008` D-1** | **`0xb1d00008` D-1** |

全アーム `STORE7=0xa1020704`（割込みトレース健全・storm 無）。

### 5.2 ★帰属の一意な結論（依頼 (a)(b)）

> **同一個体・同一アプリ・同一マーカで、blob をバイト固定（`75db98e5`＋`cb429107`）したまま
> グルーだけ hal型→C3型 に替えても、どちらも D-1 に到達する（warm・真cold とも）。
> さらに blob を v6.1 に替えた v61 も到達する。＝3供給すべて到達。**

∴ **blob 無罪・グルー無罪**（本個体・D-1 まで）。
∴ **「非収束は blob の版が原因」（§13／memory「hal matched set の BT enable サブパス版問題に
局在」）は、本個体では «反証» ではなく «前提となる現象が再現しない»**。

**★条件の明示（親指示）**：上は **warm と真cold の両方**で成立する。
**§10-13 は全て warm** だったので、**warm 同士の直接対比が成立している**（#1 の warm＝§10-12 と同一条件）。

### 5.3 ★★「では §10-12 の hal ハングは何だったのか」＝**未解決（★交絡の同定を訂正）**

> ## ★★【2026-07-17 訂正＝本節の «個体/rev 交絡» は幻だった】
> **`14:C1:9F:E0:5A:9C` こそが board C 本体**（`docs/wifi-shim-c6.md` に8回）。
> **rev v0.3 の一次情報は存在しない**（同 doc に0件。出所＝`evidence-c6-02:195` の
> «efuse blk_version» と «chip revision» の取り違え＝stock `efuse_hal.h` は別API）。
> ⇒ **本節の「個体が違う」「board C は非接続」「rev v0.2 vs v0.3」は全て誤り。**
> ⇒ **§10-12 で hal がハングしたのは «この同じボード»**。**同じボード・同じ warm 条件で
> 今 hal は D-1 に到達する**（§5.1 #1）＝**差はボードではなく «ツリー（コード）»**
> ＝**「2026-07-15 以降の tree 変化」が唯一生き残った説明**（`git log` で追える）。
> **∴ 個体差を交絡候補の筆頭に置いたのは誤り。本物の交絡は cold/warm（evidence-c6-04）だった。**
> **正本＝`evidence-c6-06-w1-and-v554-d2b.md` §2／§6（tree 変化の候補）。**

（以下は訂正前の記述。履歴として残す。）


| | |
|---|---|
| §10-12 の hal ハング | **board C＝rev v0.3**（evidence-c6-02 §5.5・memory「実施88-91 は board C」） |
| 本ラウンドの hal 到達 | **本DUT＝rev v0.2** |

⇒ **個体（および rev）が違う**。**board C は本セッションで接続されていない**＝
**「rev v0.2 vs v0.3」「board C 個体固有」「07-15 以降の tree 変化」を私は分離できない**。

★**∴ 言えること／言えないこと**：
- **言える**：**本個体では 3供給すべてが D-1 に到達する**（実測）。
  **∴ blob もグルーも «本質的に D-1 不能» ではない**。
- **言えない**：「§10-12 は誤りだった」「board C でも hal は動く」。
  ★**evidence-c6-03 が «rev v0.2/v0.3 仮説は不要になった» と書いたのは cold/warm の
  文脈では正しかったが、本ラウンドの «warm 同士で hal の可否が分かれる» は
  cold/warm では説明できない**＝**個体/rev 仮説は «復活» する**（未証明）。
- **決着させるには board C の再接続が要る**（ユーザー判断・§6.3）。

### 5.4 ★予測の答合せ（§4.1・依頼 (d)）

| # | 予測 | 確度 | 実機 | 判定 |
|---|---|---|---|---|
| **Q1** v61 warm PASS | 95% | **D-1** | **★的中** |
| **Q2** v61 cold PASS | 95% | **D-1** | **★的中** |
| **Q3** hal warm **HANG** | 70% | **D-1（PASS）** | **★外れ** |
| **Q4** hal cold **HANG** | 75% | **D-1（PASS）** | **★外れ** |
| **Q5/Q6** v554（50/50・賭けない） | — | **D-1（warm・cold とも）** | **（賭けていないので的中も外れも無い）** |

★**Q3/Q4 を «70/75%» に留め、§4.2(C) で «hal が PASS してしまった場合» を事前に用意して
おいたのが効いた**。もし「hal は HANG するもの」と 95% で書いていたら、
**「v554 が動いた ⇒ グルーが犯人」と誤結論していた**（実際は hal も動く＝グルーも無罪）。
★**そして §4.2(C-2) の「真犯人は evidence-04 が直したもの」も、逆方向対照（#2）で
warm については自分で反証した**（hal は修正 OFF でも warm で D-1）。

★**`evidence-c6-01 §7.1` の予測（「ハングする側に賭ける」＝blob 起因）は反証された**。
同 §7.1 は自ら**「反証されたら §6 の判断ごと撤回すべき、という意味でこの予測は捨て身の対照」**
と書いている＝**その宣言どおり §6 を撤回する**（§5.5）。

### 5.5 ★据置き判断：**撤回**（依頼 (e)）

`evidence-c6-01 §6`「**C6 BT の供給移行は行わない**」の3根拠を実測で点検した：

| §6 の根拠 | 本ラウンドの実測 | 判定 |
|---|---|---|
| 1. 「v5.5.4統一＝**§10-12 のハング構成に戻すこと**と同義」 | **v554 は warm・真cold とも D-1**。**hal すら D-1** | **★反証**（前提の «ハング» が本個体で起きない） |
| 2. 「基盤を esp-idf へ移すと供給混成で `shared_periph_module_t` 未定義が噴出」 | **再現**（§5.6） | **★維持**（これは今も真） |
| 3. 「C6 BT が要求する blob は v6.1（submodule に無い）」 | **★反証＝submodule の v5.5.4 blob で D-1 到達** | **★反証** |

⇒ **根拠1と3は崩れた。∴「BT だけ外部 v6.1 tree が必須」は撤回**。
**`ASP3_BT_IDF_V554=ON`（submodule 供給）で C6 BT は D-1 に到達する**
＝**外部 v6.1 tree（`/home/honda/tools/esp-idf-v6.1`＝submodule でない＝provenance の罠）への
依存を外せる**。**根拠2（基盤の混成）は別問題として残る**（§5.6）。

### 5.6 ★供給移行の進捗と hal 参照（依頼 (f)）

**hal 参照数（`build.ninja` 実測）**：

| arm | hal 参照 | v6.1 参照 |
|---|---|---|
| **v554** | **92** | **0（★混入なし）** |
| v61 | 92 | （v6.1 が供給元） |
| hal | 151 | 0 |

⇒ **`ASP3_BT_IDF_V554=ON` は «BT の blob＋グルー» を submodule へ移すが、
«基盤（esp_hw_support/hal 系）» は依然 hal から採る**（92箇所）＝**「hal 参照 0」には未到達**。

**★壁（実測・そこで止めた）**：`-DASP3_BT_IDF_V554=ON -DASP3_ESPIDF_SUPPLY=ON` で
**ビルド不能**。エラー＝

```
error: unknown type name 'shared_periph_module_t'
error: unknown type name 'soc_clk_freq_calculation_src_t'
error: 'CLK_CAL_RTC_SLOW' / 'CLK_CAL_RC_FAST' / 'CLK_CAL_32K_XTAL' / 'CLK_CAL_32K_OSC_SLOW' undeclared
error: 'CLK_HAL_TAG' undeclared
```

**原因（実測）**：`esp_bt_idf61.cmake` は**基盤ソース/インクルードを `ESP_HAL_DIR` で直書き**して
おり（**47箇所**。`esp_bt.cmake` は 75箇所）、`ASP3_ESPIDF_SUPPLY=ON` は**ヘッダだけ** esp-idf へ
切替えるため**ヘッダ（esp-idf）×ソース（hal）の混成**になる。
かつ **hal は `esp_hal_<x>` に分割・esp-idf は `components/hal` に集約**＝
**単純な `ESP_HAL_DIR`→`ESP_SUP_DIR` 置換では済まず、コンポーネント対応表が要る**
（target.cmake には WiFi 用の «供給元別パス» 機構が既にあるが、`esp_bt*.cmake` は未使用）。
さらに **`shared_periph_module_t`・`clk_hal` 系は hal と esp-idf で API が実際にドリフト**している。

**⇒ «hal 参照 0» は «BT 供給の切替え» とは別の、境界のある（が小さくない）移行作業**：
`esp_bt*.cmake` の `ESP_HAL_DIR` 計122箇所を供給元別パス機構へ載せ替え＋clk/periph の
API ドリフト吸収。**親指示「壁が高ければそこで止めて報告」に従いここで停止**。

---

## 6. 結論・申し送り

### 6.1 一言

**blob もグルーも無罪。本個体では hal・v554・v61 の3供給すべてが warm・真cold で D-1 に到達する。
∴「BT だけ外部 v6.1 tree が必須」という据置きは撤回できる。**
**ただし §10-12 の hal ハング（board C・rev v0.3）が何だったかは未解決**（個体/rev の交絡）。

### 6.2 ★方法論の収穫

1. **★「対照が落ちること」を確認せずに «実験アームが動いた» を解釈するな**。
   本ラウンドは **hal 対照も動いてしまった**＝「v554 が動いた ⇒ グルーが犯人」という
   **一見自然な結論が誤り**だった。**§4.2(C) で事前に «hal が PASS する場合» を
   用意していたから捕まえられた。**
2. **★前ラウンドの «一意に決まる» 表は «前提となる現象が再現する» ことに依存している**。
   `evidence-c6-01 §7.1` の2択表は「hal＝ハング」を所与にしていたが、**その所与が崩れた**
   ⇒ **2択のどちらでもない第3の答（両方無罪）**が出た。**2択表を作るときは
   «前提が崩れる» 第3の枝を必ず書く。**
3. **★blob のバイト同一性は «実際にリンクされるパス» で `cmp` せよ**。
   私は最初 `find | head -1` で v6.1 の libphy を**別ファイルと取り違えた**（`cf9ff744` →
   正しくは `3fea0708`）。**`build.ninja` の `-L` から実パスを取る**のが正。
4. **★条件（warm/cold）を結論に必ず書く**。本ラウンドは #1 warm を **§10-12 と同一条件**に
   揃えたことで「再現しない」と言う資格を得た（evidence-04 の教訓の適用）。

### 6.3 ★残ブロッカー／ユーザー判断事項

1. **★既定を `ASP3_BT_IDF_V554=ON` へ替えるか＝ユーザー判断（本ラウンドでは替えていない）**。
   理由：**v554 の実機実績は D-1 まで**。現行既定 v6.1 は **D-2b/D-2c まで**実績がある
   （§14/§15・board C）。**替えるなら先に v554 で D-2b（`ble_host_smoke_c6`＝NimBLE sync→adv）
   を確認すべき**＝**次の1ラウンドで片付く**。
   ★**私が勝手に既定を替えないのは「D-1 の実績で D-2b の実績を上書きしない」ため。**
2. **★§10-12 の hal ハング（warm）は未解決**。
   ~~board C の再接続で「rev v0.2 vs v0.3」「個体固有」を分離できる（ユーザー判断）。~~
   **★【2026-07-17 訂正】board C ＝ 本DUT そのもの＝既に接続済み。rev v0.3 は存在しない**
   （§5.3 の訂正バナー参照）。⇒ **ユーザーの物理作業は不要**。**同じボードで hal が
   ハング（§10-12）→到達（本ラウンド）と変わった以上、残る説明は «tree（コード）変化» のみ**
   ＝`evidence-c6-06` §6 で調査。
3. **「hal 参照 0」は未達**（§5.6 の壁）。`esp_bt*.cmake` の `ESP_HAL_DIR` 計122箇所の
   載せ替え＋clk/periph API ドリフト吸収が要る。
4. `rst:` は全 warm run で **`rst:0x15 (USB_UART_HPSYS)`** を採取＝**evidence-02 の warm アーム
   非再現の «EN/RTS 経路» 側は今回も未測定**（evidence-04 の申し送りのまま）。
