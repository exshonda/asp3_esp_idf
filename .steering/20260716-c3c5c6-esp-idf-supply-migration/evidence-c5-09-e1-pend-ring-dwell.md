# evidence-c5-09 — E1：C5 の pend_ring 滞留は «実在するか»（測るだけ・修正はしない）

**作成**: 2026-07-17 ／ **担当**: E1 専任エージェント
**ミッション**: **①（`pend_ring` 周期 flush 欠如）を «修正を書く前に» 殺せるかを測る。**
**E2（flush 追加）は «やらない»。** 他エージェントのファイル（`review-ble-c5-vs-c6.md`・
`evidence-c3-*`・`evidence-c5-08`・`evidence-c6-*`・`evidence-stock-01-*`）は **読むだけ**。

---

## 0. 本ラウンドの最重要の成果（★測定 «前» に判明した。順序を守るため先に書く）

★★**レビューが①の «機構の出典» とした C3 の一次情報を読んだところ、
「機構は C3 で実測確定」は «出典の誤読» だった。**
**一次情報（`docs/bt-shim.md:2394-2410`）は «pend_ring 滞留説» を «反証» している。**
**しかも反証の手段は «100ms 周期 flush の A/B»＝①の修正そのものだった。**

⇒ ★**これは E1 の «測定» ではない（静的な出典確認）。だが E1 の事前確率を «下げる» 側に効く。**
⇒ ★**それでも E1 は実行する**（**C3 の反証は «C3 の病態» についてであり、C5 の病態は別段**＝§3.3）。

---

## 1. D1 の検算（★3チップ «同一式»・★C3 も測った＝レビューは測っていない）

**レビューの主張（D1）**：「C5 の app に pend_ring 周期 flush が無い（C5:0 / C6:2）」。
**親の補正表の «C3 idf は flush 有り?» は «親の推定»**⇒ **実測した。**

### 1.1 測り方（★非対称な grep 事故の再発防止＝3チップに «同一式» を適用）

| 指標 | コマンド |
|---|---|
| シンボル面 | `nm --undefined-only <app>.c.obj \| grep -c flush_pending` |
| 呼出し実体 | `objdump -dr <app>.c.obj \| grep -c 'R_RISCV_CALL.*flush_pending'` |
| ★**較正**（0 が «盲» でないことの証明） | `nm --undefined-only <app>.c.obj \| grep -c esp_shim` |

`nm`/`objdump` ＝ `esp-14.2.0_20241119`（IDF 標準 toolchain）。

### 1.2 結果（★**C3 は hal / idf 両アームとも測った**）

| ビルド | app | 供給 | `nm` flush | `objdump` call | ★較正（他 `esp_shim_*`） | flush |
|---|---|---|---|---|---|---|
| `build/nr_c3_ble` | `ble_host_smoke.c` | idf | **2** | **2** | 6 | ★**有** |
| `build/rb_A_ble` | `ble_host_smoke.c` | idf | **2** | — | — | ★**有** |
| `build/rb_B_ble` | `ble_host_smoke.c` | idf | **2** | — | — | ★**有** |
| `build/rb_C_hal` | `ble_host_smoke.c` | **hal** | **2** | **2** | 6 | ★**有** |
| `build/nr_c5_ble` | `ble_host_smoke_c5.c` | idf | **0** | **0** | ★**4**（≠0） | ★**無** |
| `build/nr_c6_ble` | `ble_host_smoke_c6.c` | idf | **2** | **2** | 6 | ★**有** |

★**較正が効いている**：**C5 の obj は他の `esp_shim_*` を 4 個 undefined で参照している**
（`esp_shim_bt_clock_init`・`esp_shim_heap_free_size`・`esp_shim_initialize`・`esp_shim_int_count`）
⇒ ★**「0 を読んだが測定対象が無いだけ」ではない＝C5 の 0 は «真の不在»。**
（レビュー §5-b「0 を読んだら測定対象の存在を確かめる」の型を、私にも適用した。）

★**C3 は hal と idf で «同一 app»**（`apps/ble_host_smoke/`）＝**供給が違っても flush は同じく有る。**

### 1.3 ★親の補正表の検算結果

| セル | flush | 実測の出所 | Android bond |
|---|---|---|---|
| C3 hal | ★**有（実測）** | `rb_C_hal` = 2/2 | OK |
| **C3 idf（GCC14）** | ★**有（実測。親の «推定» を確認）** | `nr_c3_ble`/`rb_A_ble`/`rb_B_ble` = 2 | OK |
| **C5 idf** | ★**無（実測・較正済）** | `nr_c5_ble` = 0/0 | ★**到達しない** |
| C6 idf | ★**有（実測）** | `nr_c6_ble` = 2/2 | OK |

⇒ ★**親の補正表は «崩れなかった»**＝**表としては①と整合する。**
⇒ ★★**しかし §3 のとおり «整合» の中身は空である**（**flush が «有る» 2 セルは、
どちらも «flush が効いた証拠» を持たない**）。

---

## 2. 救済機構の実体（★関数名・ファイル・呼ばれ方・カウンタの更新点）

★**3 チップとも «構造同一»**（レビュー N10「pend_ring 救済機構は両方に存在」を、C3 を加えて確認）。

### 2.1 機構の実体（C5・絶対パス）

**ファイル**: `/home/honda/TOPPERS/ASP3CORE/asp3_esp_idf/asp3/target/esp32c5_espidf/wifi_v8/esp_shim.c`

| 要素 | 行 | 内容 |
|---|---|---|
| `shim_que_pend_total` | **686** | `static volatile uint32_t`＝**全キュー合計の保留数**。★**static＝app から見えない** |
| `shim_que_pend_used` | **869** | `static volatile uint32_t`＝**保留経路の «累積利用回数»**（初回のみ syslog） |
| `shim_que_pend_push_slot()` | **876-890** | ★**`pend_total++`（883）・`pend_used++`（884）**＝**唯一の増加点**。`SHIM_LOCK` 下 |
| `shim_que_pend_push()` | **897-907** | E_CTX 文脈からの送信の実体（スロット確保→copy→push_slot） |
| **`esp_shim_queue_flush_pending()`** | **916-948** | ★**救済機構の本体**。`pend_total==0` なら即 return（高速パス・924）。`psnd_dtq` で DTQ へ流す。**`pend_total--`（938）** |
| `esp_shim_sem_flush_pending()` | **504** | セマフォ側（`shim_sem_pend_total`・408） |

### 2.2 ★誰が駆動するか（＝①の «形» そのもの）

| 呼出し点 | 行 | 性質 |
|---|---|---|
| `esp_shim_exit_critical()` の**最外解除** | **158-159** | ★**機会的**（MIE 復帰時） |
| `esp_shim_queue_send()` 冒頭 | **976** | 機会的 |
| `esp_shim_queue_recv()` 冒頭 | **1079** | 機会的 |
| `esp_shim_queue_delete()` | **1117** | 破棄前の精算 |
| ★**app の 100ms 定期ループ** | ★**C5 は «無し»**／C3 `ble_host_smoke.c:956-957`／C6 `ble_host_smoke_c6.c:918-919` | ★**周期的** |

⇒ ★**レビューの «機構はあるのに、それを定期的に駆動する側だけが app に無い» は «構造として» 正しい**（実測で確認）。

### 2.3 ★★ただし «機会的 flush» は死んでいない（①の前提を弱める側の事実）

★**`exit_critical` の最外解除ごとに flush が走る**（158-159）。
**BT クリティカルセクションは接続イベントごとに出入りする**⇒ **drain の機会は «ほぼ連続»。**
⇒ ★**「app の周期 flush が無い＝滞留する」は «自明には» 成り立たない。**
**この «窓» が実際に開くかどうかが、まさに E1 が測るもの。**

---

## 3. ★★「30秒」の出典（★一次情報を読んだ。★レビューの読みは «誤り»）

### 3.1 レビューが引いた文（`review-ble-c5-vs-c6.md` §7-E1・**読むだけ**）

> 「…NimBLE の SM proc が対向 PDU を待って最終的に 30秒で `BLE_HS_ETIMEOUT` する
> （docs/bt-shim.md「D-2d bond診断」で**暗号後の Identity PDU 滞留＝30秒待ちを実測確定**）」
> ＝**これは «安全網» と呼ばれているが、由来は実測された実バグの修正**である。

★**この引用元は «一次情報» ではなく、C3 app のコメント**（`apps/ble_host_smoke/ble_host_smoke.c:944-955`）。

### 3.2 ★一次情報（`docs/bt-shim.md:2394-2410`）が «実際に» 言っていること

**節題**：「**追加の決定的実験（同日）：enc→ETIMEOUT 実測=30秒＋周期flush修正が «無効» ＝
真因を controller/LL の «暗号化ACL鍵配布 未配送» へ確定**」

| 一次情報が実測したこと | 引用（逐語） |
|---|---|
| ★**30秒は «実測»** | 「**Enc→ETIMEOUT=30秒（実測）** → NimBLE の SM タイマは «正確に30秒» 発火」 |
| ★★**pend_ring 滞留説は «反証»** | 「**100ms 周期 flush でも bond は成立せず**（iPhone/Android とも同じ30秒 ETIMEOUT・PAIRING_COMPLETE 不発）→ **pend_ring 滞留説は反証**（滞留していれば ≤100ms flush で必ず host へ届き PAIRING_COMPLETE が発火するはず）」 |
| ★★**shim は «無罪» と明記** | 「**shim（pend_ring/flush）は無罪**」 |
| ★★**flush は «安全網として残置»** | 「計装の 100ms flush は «防御的安全網» として残置（**bond は直さないが** pend_ring 滞留一般への保険。pend残0で即return＝非回帰）」 |

### 3.3 ★★★ 判定：「30秒」は何の数字か / C5 に適用できるか

| 問い | 答（実測に基づく） |
|---|---|
| **「30秒」はどこから来た数字か** | ★**NimBLE の SM proc タイマ（`BLE_SM_TIMEOUT`）の定数 30 秒**。C3 で **Enc→ETIMEOUT の実秒差＝30秒** として計装実測された（`0x5de01e0d` の byte1=0x1e=30）。★**「SM が対向 PDU を待って正当に 30 秒待った」ことの証拠**であって、★**«pend_ring に滞留した» ことの証拠ではない。** |
| **«何が» 実測されたのか** | ★**(a) 30 秒待ちの «存在»**（実測・真）／★**(b) pend_ring 滞留説の «反証»**（実測・①にとって不利）／(c) 真因は別（下記） |
| **C5 にそのまま適用できるか** | ★★**できない。二重に。**<br>**(i) 病態が違う**：C3＝**暗号成立«後»に ETIMEOUT(13)**／C5＝**暗号が成立せず ENOTCONN(7)**＝**ENC_CHANGE の «前» で終わる**。**C3 の 30 秒は «暗号後の鍵配布待ち» の数字＝C5 の «暗号前に対向が降りる» には対応しない。**<br>**(ii) C3 の真因は既に別に確定し、既に «直っている»**：`docs/bt-shim.md:2646`＝**`BLE_HS_PVCY=0` が responder の Identity 鍵配布をコンパイルアウトしていた**。★**私が実測**：**現行の C5/C6 とも `CONFIG_BT_NIMBLE_HS_PVCY 1`**（`bt/stub/include/bt_nimble_config.h:127` / C6 `:130`）＝**ELF に `ble_hs_pvcy_*` が 9/9 個 present**（`nr_c5_ble`・`nr_c6_ble` とも）。**⇒ C3 の 30 秒事件の真因は C5 には «存在しない»。** |

### 3.4 ★★①の «根拠» に対する含意（★これは «測定» ではなく «出典の訂正»）

★**レビューの「機構は C3 で実測確定」は成り立たない。** 正確には：

1. ★**C3 で実測されたのは «30 秒待ちの存在» であって «pend_ring 滞留» ではない。**
2. ★★**pend_ring 滞留説は C3 で «直接介入» により反証された**
   ——**しかもその介入は «100ms 周期 flush の追加»＝①の修正（E2）そのもの**。
   ⇒ ★★**E2 は «C3 で既に 1 回実施され、効かなかった»。**
3. ★**C3 app の flush は «効いたから残っている» のではなく «効かなかったが保険として残された»。**
4. ★**C6 の flush はそもそも «予防»**——C6 app のコメント自身が
   （`ble_host_smoke_c6.c:909-916`）「**…滞留する リスクへの保険**」「C3 f9dae7d/1190be9 と同一の設計判断」
   と書く＝**C6 で滞留が実測された記録は無い。**

⇒ ★★★**親の補正表の «flush 有 → bond OK» の 3 セルは、いずれも «flush が効いた» 証拠を持たない**：
   **C3 の 2 セル＝flush は «bond を直さなかった» と実測済／C6＝flush は «予防的»。**
   ⇒ ★**表の «整合» は «flush が効いている» ことを意味しない**（**相関ですらなく、«残置された保険» の分布**）。
   ⇒ ★**①の «事前確率» は、レビューの 35%／親の «35%より高いはず» の «どちらよりも低い» と私は判断する。**

★★**ただしこれは①を «殺さない»**：
- **C3 の反証は «C3 の病態»（暗号後 ETIMEOUT）についてのもの。C5 の病態（暗号前 ENOTCONN）は別段。**
- **C3 の反証当時は `PVCY=0` という «別の真因» が同時に存在した**
  ⇒ **「flush を足しても直らなかった」は «PVCY=0 が支配的だったから» でも説明がつく**
  ＝★**C3 の反証実験は «交絡» していた可能性がある**（★**反証条件も仮説＝独立に検算する、の適用**）。
- ⇒ ★**だから «測る»。E1 は依然として «情報量がある»。**

---

## 4. ★E1 の設計（★計器は «ミラーのみ»）

### 4.1 何を測るか

| 量 | 意味 | ①にとっての役割 |
|---|---|---|
| ★**dwell high-water**＝`shim_que_pend_total` を **独立タスクから 200ms 周期でサンプルした最大値** | ★**「保留リングに «居座った» 個数」**＝**滞留そのもの** | ★**主判定** |
| ★**`shim_que_pend_used`**＝**保留経路の累積利用回数**（既存カウンタ） | **「E_CTX フォールバック経路が «そもそも使われたか»」** | ★★**較正**（下記） |

### 4.2 ★★なぜ `pend_used` を «必ず» 同時に測るか（＝0 を «盲» と区別するため）

★**dwell high-water == 0 は、2 つの «全く違う» 状態と両立する**：
1. **保留経路は使われたが、常に即座に flush された**（＝**滞留は無い**＝★**①が死ぬ**）
2. **保留経路が一度も使われていない**（＝**測定対象が存在しない**＝★**①について何も言えない**
   ＝**レビュー §5-b「0 を読んだら測定対象の存在を確かめる」の型**）

⇒ ★**`pend_used` がこの 2 つを分ける。** ★**`used>0 かつ hw==0` で初めて「滞留は実在しない」と言える。**
⇒ ★**`used==0` なら、E1 は «①を殺す» のではなく «①の前提が土俵に乗っていない» を意味する**
（**その場合、①は «C5 の Android 失敗» の説明として «機構ごと不在»＝別の意味で死ぬ**）。

### 4.3 ★なぜ «サンプル» で測るか（＝push 点の high-water では «滞留» を測れない）

★**push 点で `pend_total` の最大値を取っても、それは «滞留» ではない**：
**push 直後の `exit_critical` で即 flush されれば `0→1→0` となり、push 点 high-water は 1 を記録するが
«滞留時間はマイクロ秒»。** ⇒ ★**push 点 high-water は «経路が使われたか»（＝`used`）と実質同義。**
⇒ ★**滞留＝«居座り» を測るには «独立した時計から覗く» しかない**＝**200ms サンプラ**。
★**①が主張する滞留は «30 秒スケール»**（SM が対向 PDU を待つ）⇒ **200ms サンプラなら ~150 回捕捉できる**
＝**計器と仮説のスケールが整合している。**

★**正直な限界**：**200ms «未満» の滞留は取りこぼす。** だが★**そのような滞留は①が主張する病態
（対向がペアリング中に諦める＝秒スケール）を作れない**⇒ **仮説に対しては十分な感度。**

### 4.4 計器の実体（★最小・hot path に置かない）

- **shim 側**：`static` を app から読むための **アクセサ 1 個のみ**を追加（`TOPPERS_C5_PEND_DIAG` 既定 OFF）。
  ★**push/flush の hot path には «1 命令も» 足さない。**
- **app 側**：**既存の `storm_monitor_task`（200ms 周期・`MONITOR_PRIORITY=15`）**でサンプルし、
  **LP_AON へ 1 レジスタ書込み**（既存の STORE4 書込みを置換＝**書込み回数は増えない**）。
- ★**`--wrap` は «使わない»**（噛んだかの確認が要る計装を避ける＝レビューの規則）。

### 4.5 ★マーカー（★空きレジスタを «実測で» 選んだ）

★**C5 の LP_AON STORE は «0-9 の 10 本のみ実在»**——★**推定ではなく実測**：
`esp-idf/components/soc/esp32c5/register/soc/lp_aon_reg.h:17-125` に **`LP_AON_STORE0_REG`〜`STORE9_REG` の 10 本だけ**が定義（`STORE10/11` は非実在＝app の既存コメントが正しい）。

| STORE | addr | 現用途 | 空きか |
|---|---|---|---|
| 0 | `0x600B1000` | sync | 使用中 |
| 1 | `0x600B1004` | RTC_SLOW_CLK_CAL（実施35） | ★**予約＝触るな** |
| 2 | `0x600B1008` | adv rc | 使用中 |
| 3 | `0x600B100C` | ble_hs reset | 使用中 |
| ★**4** | ★**`0x600B1010`** | ★**割込み線1累積ミラー**（`esp_shim_int_count[1]`） | ★★**実質空き**（下記） |
| 5 | `0x600B1014` | WRITE(`0xABF3`) マーカ | 使用中（**証拠**） |
| 6 | `0x600B1018` | ★**ENC/PAIR 共用**（`0x5DC0` 観測不能） | 使用中（**証拠**） |
| 7 | `0x600B101C` | bt_shim 割込みトレース | 予約 |
| 8 | `0x600B1020` | CONNECT | 使用中（**証拠**） |
| 9 | `0x600B1024` | DISCONNECT | 使用中（**証拠**） |

★**STORE4 を選ぶ根拠**（★**私の推定ではなく、他エージェントの «実測» の引用＋構造の確認**）：
`evidence-c5-08 §11`（**読むだけ**）は「**`STORE4`（割込み線1累積ミラー）は本ラウンド 3セルとも
`0x00000000`＝情報価値が尽きている**⇒ **`STORE4` を明け渡すのが最小コスト**」と記録し、
**§8.1 の実測表でも `0x600B1010 = 0x00000000`**。
⇒ ★**STORE4 は «常に 0 を書いている»＝上書きしても失う情報が無い。**

★★**副次的に解ける曖昧さ**：**現行の `STORE4=0` は «int_count[1]==0» と «monitor task が死んでいる» を
区別できない**（**どちらでも 0 になる**）。★**私のマーカーは «タグ `0xE1`» を無条件に書く**
⇒ ★**タグが読めれば «サンプラが生きている» ことの証明**＝**この曖昧さを閉じる。**

### 4.6 ★エンコード（★タグで引く・3 フィールドのみ＝誤読しにくく）

```
STORE4 (0x600B1010) = 0xE1 << 24 | (dwell_hw & 0xFF) << 16 | (pend_used & 0xFFFF)
```

| bit | 意味 | 飽和 |
|---|---|---|
| `[31:24]` | ★**タグ `0xE1`**（＝E1 実験・**サンプラ生存の証明**） | — |
| `[23:16]` | ★**dwell high-water**（200ms サンプルの最大 `shim_que_pend_total`） | 255 |
| `[15:0]` | ★**`pend_used`**（保留経路の累積利用回数） | 65535 |

★**読み方**：
- `0x00000000` ⇒ ★**サンプラが動いていない**（**E1 は «測定不成立»＝結果を語るな**）
- `0xE1_00_0000` ⇒ **used=0**＝★**保留経路が一度も使われていない**（**①の機構が土俵に乗らない**）
- `0xE1_00_xxxx`（used>0・hw=0） ⇒ ★★**経路は使われたが «滞留は無い»＝①は死ぬ**
- `0xE1_hh_xxxx`（hw>0） ⇒ ★**滞留が実在＝①は生き残る**（**E2 は «別ラウンド»＝今回やらない**）

---

## 5. ★★事前登録（★測定 «前» に commit する。★以後 書き換えない）

### 5.1 予測（数値）

| # | 予測 | 登録値 |
|---|---|---|
| ★**P_E1** | ★**C5 × Android の bond 失敗時に pend_ring 滞留が «実在する»（dwell hw > 0）** | ★**15%** |
| **P_USED** | **保留経路がそもそも使われる（`pend_used > 0`）** | **70%** |
| **P_C0** | **計装込みビルドで C5 × iPhone が bond する（＝計装は非破壊）** | **85%** |
| **P_ALIVE** | **タグ `0xE1` が読める（＝`storm_monitor_task` が生きている）** | **90%** |

### 5.2 ★P_E1 を 15% にした理由（★レビュー 45%／親「35%より高いはず」の «どちらよりも低い»）

★**下げた理由（★§3 の一次情報が主）**：
1. ★★**pend_ring 滞留説は C3 で «直接介入»（100ms flush＝E2 そのもの）により反証済み**
   ——**レビューの «機構は実測確定» は出典の誤読**（§3.2）。
2. ★★**«flush 有» の 3 セルは «flush が効いた» 証拠を 1 つも持たない**
   （**C3=効かなかったと実測／C6=予防的**）＝**親の補正表の «整合» は中身が空**（§3.4）。
3. ★**機会的 flush が死んでいない**：`exit_critical` の最外解除ごとに走り、
   **BT クリティカルセクションは接続イベントごとに出入りする**＝**drain 機会はほぼ連続**（§2.3）。
4. ★**C5 の病態は «暗号前»（ENOTCONN）**＝**C3 の «暗号後の鍵配布待ち»（30 秒）とは段が違う**。
   ①の物語（SMP PDU が滞留 → SM が待つ）は **«暗号後の鍵配布» を想定した機構**であり、
   **C5 の «暗号に到達しない» を素直には説明しない**。
5. ★**`memory` の «C5×iPhone は flush 無しで OK»**＝**flush 欠如は単独で非対称を説明しない**
   （レビュー自身の正直な自己評価 §9）。

★**0 にしない理由（＝①を «測る前に» 殺さない）**：
1. ★**C3 の反証実験は «交絡» していた可能性**：当時 `PVCY=0` という **別の支配的真因**が同時に存在した
   ⇒ **「flush を足しても直らなかった」は «PVCY=0 のせい» でも説明がつく**
   ＝★**反証条件そのものを疑う**（§6.2 で独立に検算する）。
2. ★**C5 は 4 セル中で «唯一» 周期 flush を持たない**——**構造上の非対称は実在する**（§1.2 で実測）。
3. ★**原因が «我々の統合» に確定した**（`evidence-stock-01 §20`＝**読むだけ**）
   ⇒ **①は «我々の app»＝射程内**。
4. ★**stock 対照は «shim/OSAL 層が存在しない»**⇒ **この層は «未計測の 4 面» の 1 つ**（同 §22.1）。

### 5.3 ★★分岐が «何を意味するか»（★結果を見る «前» に固定する）

| 実測 | 判定 | 次 |
|---|---|---|
| ★**dwell hw == 0 かつ used > 0** | ★★**①は死ぬ**（**滞留が無いのに «flush の欠如» を語れない**） | ★**②③④か、未知の第5の面へ。E2 は «書かない»** |
| ★**dwell hw == 0 かつ used == 0** | ★**①は «機構ごと» 土俵に乗らない**（**E_CTX フォールバックが発動していない**） | ★**①は «C5×Android の説明» としては死ぬ**。ただし «滞留は無い» とは別の理由 |
| ★**dwell hw > 0** | ★**①は生き残る** | ★**E2（flush 追加）だが «別ラウンド»＝今回やらない。ユーザー判断** |
| **タグ `0xE1` が読めない** | ★**測定不成立** | ★**結果を語るな。計器を直してやり直す** |
| **C0 が壊れる（iPhone が bond しない）** | ★**E1 へ進まない** | ★**計装が原因＝この計装で Android を語ってはいけない** |

★★**どちらでも収穫。結果を «期待しない»。**

### 5.4 ★★反証条件の «検算»（★反証条件も仮説だから独立に測る）

★**本リポジトリには «反証条件自体が invalid だった» 実例がある**（`evidence-c5-08 §11`＝
**「`0x5DC0` の不在＝PAIRING 未到達」という判別子が «原理的に観測不能» だった**）。
⇒ **私の反証条件（「hw==0 ⇒ ①が死ぬ」）を独立に検算する**：

| 検算すべき前提 | どう検算するか | §6 で実施 |
|---|---|---|
| **(a) サンプラは生きているか** | ★**タグ `0xE1` の存在**（無条件書込み） | ★実施 |
| **(b) `pend_used` は «増えうる» か**（＝カウンタが死んでいない） | ★**C5×iPhone（C0・健全セル）でも同じマーカーを読む**＝**bond «成功» セルで used>0 なら、カウンタは «動く»** | ★実施 |
| **(c) STORE4 は真cold で «消える» か**（＝前セッションの残留を新観測と誤読しない） | ★**真cold の «前» に sentinel を書き、«後» に消えたことを読み戻す** | ★実施 |
| **(d) 私の «高々 200ms サンプル» は 30 秒滞留を捕捉できるか** | ★**構造的に自明**（30s / 200ms ≈ 150 サンプル）。ただし «サンプラが飢餓しない» ことは (a) が担保 | ★実施 |

★**(b) が «C0 を単なる非破壊検定以上のもの» にする**：**C0 は同時に «計器の陽性対照» になる。**

### 5.5 ★射程（★これを超えて書かない）

★**E1 が語れるのは «C5 × Android が bond に到達しない» の 1 軸«のみ»。**
★**外挿してはいけない 2 軸**（`evidence-stock-01 §22.3`＝**読むだけ**が «別の病態» と明記）：
- **C3 × Android＝bond するが «再接続» できない**
- **C3 × iPhone＝wedge**
★**①が説明しうるのは C5 の 1 軸のみ。1 つの fix が 3 つを直すと «仮定しない»。**

---

## 6. 計器の実装と «出力側» の検定（★§5 は書き換えていない）

### 6.1 変更点（全列挙・★submodule は 1 行も触っていない）

| ファイル | 変更 |
|---|---|
| `asp3/target/esp32c5_espidf/wifi_v8/esp_shim.c` | **読むだけのアクセサ `esp_shim_pend_stats()` を 1 個追加**（`TOPPERS_C5_PEND_DIAG` 既定 OFF）。★**push/flush の hot path は 1 命令も変えていない** |
| `apps/ble_host_smoke_c5/ble_host_smoke_c5.c` | `storm_monitor_task`（既存・200ms）で サンプルし STORE4 へ 1 書込み（**書込み回数は従来と同一**）＋`extern` 宣言 |
| `asp3/target/esp32c5_espidf/esp_bt.cmake` | `option(ESP32C5_BT_PEND_DIAG ... OFF)` |

### 6.2 ★計器が «本当に入ったか» を出力で検定（cache ではなく ELF/ninja）

| 検定 | 結果 |
|---|---|
| アクセサがリンクされたか | ★`nm --defined-only` = `42001dac T esp_shim_pend_stats` |
| app が **実際に呼ぶ** か | ★`objdump -dr` の `R_RISCV_CALL` = **1** |
| `-D` が実効か | `build.ninja` に `TOPPERS_C5_PEND_DIAG` = **128 行** |
| タグ定数が入ったか | ★`storm_monitor_task` 内に `lui s3,0xe1000`（＝`0xE1000000`）・`lui s1,0x600b1`（＝STORE4） |
| ★**陰性対照** | ★**計装 OFF ビルド（`build/e1_c5_off`）はアクセサ 0 個・`-D` 0 行**＝**ゲートは完全** |
| ビルド | ON/OFF とも **rc=0**。`.text` ＝ **OFF 417696 B / ON 417760 B ＝ +64 B**（計器の実コスト） |

★**「差分バイト数」は指標として使わない**：64 B 挿入で以降の全コードがシフトするため
`cmp -l` は 40 万バイト差と出る＝**無意味な数**（第8再発「単位なき数値」）。**意味があるのは +64 B のみ。**

★**vintage の申告（no silent caps）**：**私の OFF ビルドは歴史的アーム B（`c5_tc_B_ble`）と
同一ではない**（`.text` 417696 vs **417712**＝**-16 B**）。**他エージェントの `esp_timer.h` 修正
（15:09:49）以降のソースで建てたため**（`evidence-c5-08 §10.1.1` が予告した vintage ドリフト）。
⇒ ★**静的一致では «非回帰» を主張しない。C0 を実機で測って主張する**（§6.3）。

### 6.3 ★★C0（計装の非破壊性）＝ **PASS（2/2・真cold）**

★**健全な対照に BlueZ を使った**（親の許可「BlueZ でも可なら先にそちらで」）。
**根拠＝`memory/c5-ble-bond-persistence`・`c5-ble-d2c-d2d`＝C5×BlueZ は bond «成功» する健全セル。**
**ハーネスは確立済みの型**（**D-Bus 直叩き**・**`NoInputNoOutput` agent を自分で登録**・
**毎回 `remove` してフレッシュ bond**）。

| run | 真cold の証明 | 開始時 | 結果 | ★`0xABF4`（暗号必須 READ） |
|---|---|---|---|---|
| **1** | `usbjtag=GONE cp2102n=GONE` | `paired=False bonded=False` | ★**`paired=True bonded=True`** | ★**`b'BT4-OK'`** |
| **2** | `usbjtag=GONE cp2102n=GONE` | `paired=False bonded=False` | ★**`paired=True bonded=True`** | ★**`b'BT4-OK'`** |

★★**`0xABF4` が返る＝bond で確立した LTK が «実効»**（未ペアなら返らない）
＝**«paired フラグ» ではなく «暗号が実際に効いている» ことの end-to-end 証明。**
⇒ ★**計装は健全セルを壊していない＝E1 へ進んでよい。**

### 6.4 ★C0 セルのマーカー実測（2 run とも **完全一致**）

| reg | 値 | 意味 |
|---|---|---|
| STORE0 `0x600B1000` | `0x5ade51c0` | SYNC 到達 |
| STORE2 `0x600B1008` | `0xad000000` | adv rc=0 |
| STORE3 `0x600B100C` | `0x00000000` | `ble_hs` reset **無し** |
| ★**STORE4 `0x600B1010`** | ★**`0xe1000000`** | ★**tag=0xE1（サンプラ生存）・dwell_hw=0・pend_used=0** |
| STORE5 `0x600B1014` | `0x00000000` | WRITE 未到達（ハーネスが `0xABF3` を書いていないため＝**想定どおり**） |
| ★**STORE6 `0x600B1018`** | ★**`0x5de00000`** | ★**ENC_CHANGE status=0＝暗号成立**（＝BlueZ の `paired=True` と 2 系統一致） |
| STORE7 `0x600B101C` | `0xa1020704` | bt_shim 割込みトレース（予約） |
| STORE8 `0x600B1020` | `0x604e0001` | CONNECT status=0・count=1 |
| STORE9 `0x600B1024` | `0xd15c1301` | DISCONNECT reason byte=0x13・count=1（**我々のハーネスが切断した**） |

---

## 7. ★★★ 予期しなかった結果：**健全セルで `pend_used == 0`**＝**私の事前登録した較正が «空振り» した**

★**§5.4 の検算(b) は「健全セルで `used>0` ならカウンタは動く」と設計した。実測は `used==0`。**
⇒ ★★**較正が成立しなかった**＝**「hw==0 は滞留が無いから」なのか「私の計器が盲」なのかを
この対照では分けられない**（＝**私自身が §4.2 で警告した罠に、私の計器が落ちた**）。
⇒ ★**別の手段で «計器が盲でない» ことを証明する必要が生じた。**

### 7.1 ★代替の較正（★3 本立て・すべて «出力» 側）

| # | 問い | 実測 | 判定 |
|---|---|---|---|
| ★**(1)** | **私のアクセサは «増分される変数と同じ番地» を読んでいるか** | ★**逆アセンブルで一致を確認**：<br>アクセサ `esp_shim_pend_stats` は `lw a5,-1880(gp) # 40800870 <shim_que_pend_total>` / `lw a5,-1884(gp) # 4080086c <shim_que_pend_used>`<br>増分側 `shim_que_pend_push_slot` は `addi a6,gp,-1880 # 40800870` → `sw a1,0(a6)` / `addi a3,gp,-1884 # 4080086c` → `sw a1,0(a3)` | ★★**同一番地**＝**アクセサは «増分が書く、まさにその記憶» を読んでいる** |
| ★**(2)** | **E_CTX フォールバックは «リンクされ・到達可能» か**（＝死んだコードを測っていないか） | ★`shim_que_pend_push_slot` への **`jal` が 2 箇所**：`42001e9c`（`esp_shim_queue_send` 内）・`42001f76`（`esp_shim_queue_send_from_isr` 内） | ★**機構は実在し到達可能** |
| ★★**(3)** | **その機構は «BLE の経路上» にあるか**（＝そもそも無関係な計器を見ていないか） | ★★**NimBLE の `npl_freertos_eventq_put` が実際に shim を呼ぶ**：`jal 42001ebe <esp_shim_queue_send_from_isr>`（`sns_ctx` 判定後）／`jal 42001dbe <esp_shim_queue_send>`。**`esp_shim_queue_send` の呼出し元＝`npl_freertos_eventq_put` / `_put_to_front` / `_remove`** | ★★**pend 機構は BLE イベント経路上に «在る»**＝**計器は正しい対象を見ている** |

⇒ ★★★**(1)(2)(3) により `pend_used == 0` は «真の測定»**：
**機構は BLE 経路上に実在し到達可能で、私の計器はその機構の記憶を直接読んでいる。
それでも 0 ＝ `shim_que_pend_push_slot` は «一度も実行されていない».**

### 7.2 ★機構が «いつ» 発動するのか（ソースで確認＝結果の解釈に必須）

`esp_shim.c:1011-1025`（`esp_shim_queue_send`）／`:1062-1079`（`_from_isr`）：

```
_from_isr:  pol_sem → E_CTX なら slot確保 → psnd_dtq → ★E_CTX «なら» pend_push_slot
send:       twai_sem → E_CTX なら pol_sem → ★pol_sem も E_CTX «なら» pend_push
```

⇒ ★★**pend_ring は «最後の手段»＝`psnd_dtq` «すら» E_CTX を返す文脈（MIE==0＝CPU ロック／
BT クリティカルセクション内）でのみ発動する。** **通常の ISR 文脈（MIE==1）では `psnd_dtq` が
成功するので pend_ring は «使われない».**
⇒ ★**`used==0` の意味＝「この構成では BT コントローラは eventq_put を «MIE==0 の文脈から» 呼んでいない」。**

### 7.3 ★★C0 が «想定以上に» 情報を持ってしまった（★ただし射程は守る）

★**健全セル（BlueZ）は «接続＋ペアリング＋SM＋暗号確立＋暗号必須 GATT READ» の
フルセッションを通っている**（`0xABF4=BT4-OK` が証拠）。**その全行程で `pend_used==0`。**
⇒ ★**少なくとも «成功する BLE セッション» では、E_CTX フォールバックは 1 度も発動しない。**

★★**しかしこれは E1 の答えではない**：
- **E1 の問いは «C5 × Android の «失敗時»» に滞留があるか。**
- ★**Android セルは «失敗する» セル＝コントローラの振る舞いが違う可能性を排除できない**
  （**RPA・タイミング・対向が降りる**）。
- ⇒ ★**«BlueZ で 0 だったから Android でも 0» は «外挿»＝やらない。Android セルを測る。**

---

## 8. ★E1 セル（stock ではなく **我々の ASP3**）＝ **準備完了・ユーザー確認待ち**

### 8.1 なぜ Android セルに «スマホが要る» か（設計の理由＝親の要求）

★**C5 × BlueZ は bond «成功» する**（§6.3 で 2/2 実測）＝**BlueZ は健全セル。**
★**失敗するセントラルは Android «だけ»**（`evidence-c5-08 §8`＝**読むだけ**）。
⇒ ★**「滞留を «失敗時に» 測る」には Android が要る。BlueZ では失敗を再現できない。**
⇒ ★**C0（非破壊性）は BlueZ で足りた**ので、**ユーザーの手間は «Android 1 セル» だけに絞った。**

### 8.2 載っているもの（★独立に読み戻して確認）

| 項目 | 実測 |
|---|---|
| 個体 | ★**MAC で同定＝`d0:cf:13:f0:a7:44`**（ESP32-C5 rev v1.0）＝**ポート番号で焼いていない** |
| ビルド | `build/e1_c5_pend`（**アーム B ＝ IDF 標準 `esp-14.2.0_20260121` / rv32imac**・`ESP32C5_BT_SM=ON`・**`ESP32C5_BT_PEND_DIAG=ON`**） |
| 書込み | `Hash of data verified` |
| 広告 | ★**`ASP3-C5-BLE @ D0:CF:13:F0:A7:44` をスキャンで実測**（**スキャナ較正＝7 デバイス見えている＝死んだ検出器ではない**） |

### 8.3 ★真cold の証明 ＋ 1セル1ボード

| 項目 | 実測 |
|---|---|
| ★**真cold** | `uhubctl -l 1-6 -p 3-4 -a off` → ★**`usbjtag=GONE cp2102n=GONE` を読み戻し**（`rst:0x1` は使っていない） |
| ★**1セル1ボード** | ★**C3（port1）・C6（port2）を電源断し `Port 1: 0000 off` / `Port 2: 0000 off` を読み戻し**＋**by-id 消滅**＋★**スキャンで他の `ASP3-*`/`IDFCTL-*` が «居ないこと» を実測** |
| ★**hub 1-5（別プロジェクト）** | ★**一切触っていない** |
| ★**BlueZ の後始末** | ★**DUT の bond/device object を `remove`・`Discovering=False`**＝**BlueZ が再接続してセルを奪わないようにした** |

### 8.4 ★★staleness（残留）の制御＝**構造的に閉じている**（★sentinel で独立に検算した）

| # | 前提 | 実測 |
|---|---|---|
| ★**(c1)** | **LP_AON は POR で消えるか** | ★**sentinel 実測**：STORE3 へ `0xDEADBEEF` を書込み→読み戻して確認→**真cold**→★**`0x00000000`**＝**POR は LP_AON を消す**<br>★**交絡の検算**：本ビルドで STORE3 を書くのは `on_reset()` «のみ»（**bond診断 `0xB0D5`＝ELF に 0 件・APIERR_TRACE＝`-D` 0 行**）で、**`on_reset` は発火していない**（STORE3=0）⇒ **消したのはアプリではなく POR** |
| ★★**(c2)** | **`used`/`hw` は前回ブートの値でありうるか** | ★★**ありえない（構造的）**：`nm` で **`shim_que_pend_total`/`shim_que_pend_used` はどちらも `b`＝`.bss`**（`0x40800870`/`0x4080086c`・`.bss` は `0x40800800`+）＝**毎ブート 0 初期化される RAM**。`hw` は `storm_monitor_task` の**ローカル変数**。⇒ ★**«前ブートの滞留» を今ブートの値として読むことは原理的に不可能** |
| ★**(c3)** | **タグが読めた＝今ブートが書いた、と言えるか** | ★**(c1) より POR が LP_AON を 0 にする**⇒ **真cold 後に `0xE1……` が読めたなら、それは «今ブートの `storm_monitor_task` が書いた» 以外にありえない** |

★**第9再発（残留マーカーの検定）の型どおり、«値のもっともらしさ» ではなく
«操作履歴＋コード自身の初期化挙動» で決着させた。**

### 8.5 ★ユーザーへ渡す手順（★Android «のみ»・判定条件を明示）

★**現在の状態＝真cold 済・`ASP3-C5-BLE` で広告中・C3/C6 は電源断・BlueZ は切り離し済。**

1. ★★**Android と iPhone の «両方» で、`ASP3-C5-BLE` を «この端末の登録を解除»（forget）する**
   （★**bond は RAM backed＝真cold のたびにデバイス側は鍵を失う**。**片方でも古い bond が残ると
   «9時間溶かした» 型の事故になる**。**iPhone も忘れさせる**＝**iPhone が勝手に繋いでセルを消費しないため**）。
2. **Android の Bluetooth 設定で `ASP3-C5-BLE` を «ペアリング» する。**
3. **観測して報告してほしいこと（逐語で結構）**：
   - **ペアリング要求が出たか**
   - **「登録されました」か「登録されていない／失敗」か**
   - **切れたなら «どのくらいで» 切れたか**（**体感で結構＝«すぐ» / «数秒» / «30秒くらい»**）
     ★**この «時間» に意味がある**：**①が正しいなら NimBLE の SM は «正確に 30 秒» 待って
     timeout する**（`docs/bt-shim.md` の C3 実測）。**«すぐ切れる» なら 30 秒待ちですらない。**
4. ★**iPhone は «触らない»**（**1 ビルド 1 端末＝私の STORE4 は累積値なので、
   2 端末を同一ブートで測ると混ざる**）。

★★**私はユーザーが試す «前» にマーカーを読まない**（親の指示）。**報告を受けてから読む。**

### 8.6 ★★判定（★§5.3 の事前登録どおり。★書き換えない）

**読むのは `STORE4 = 0x600B1010`**：`0xE1 | dwell_hw(8) | pend_used(16)`

| 実測 | 判定 |
|---|---|
| `0xE1_00_0000`（hw=0・used=0） | ★**①は «機構ごと» 土俵に乗らない**＝**E_CTX フォールバックが発動していない**⇒**①は C5×Android の説明として死ぬ** |
| `0xE1_00_xxxx`（used>0・hw=0） | ★★**①は死ぬ**（**経路は使われたが滞留しない**） |
| `0xE1_hh_xxxx`（hw>0） | ★**①は生き残る**⇒**E2 は «別ラウンド»＝今回やらない・ユーザー判断** |
| `0x00000000` | ★**測定不成立**（サンプラが動いていない）＝**結果を語らない** |

★**同時に読む «帰属の検算»**：**STORE6/8/9 が既知ベースライン
（`ENC=0x5de00007`・`CONN=0x604e0001`・`DISC=0xd15c13xx`）と一致するか。**
★**一致すれば「計装は病態を変えていない」の独立証拠**（＝**失敗セル側の C0**）。
★**一致しなければ「計装が病態を変えた」可能性＝その場合 E1 の結果で①を語ってはいけない。**

### 8.7 ★未測定・取らなかった対照（no silent caps）

- ★**C0 に iPhone を «使わなかった»**。**理由＝BlueZ が健全セルとして成立し（bond＋`0xABF4` が 2/2）、
  親の「BlueZ で足りるならスマホを使うな」に従いユーザーの手間を減らした。**
  ★**限界＝BlueZ は «RPA を使わない» セントラル**（`memory/c5-ble-bond-persistence`＝peer は public）
  ⇒ ★**「RPA を使うセントラルでも計装が無害」は «測っていない»。**
  ★**ただし §8.6 の «失敗セル側の C0»（ベースライン 9 レジスタ一致）が Android で これを補う。**
- ★**`shim_sem_pend_total`（セマフォ側の保留）は «測っていない»**。**理由＝①の主張は
  `esp_shim_queue_flush_pending`（キュー側）であり、STORE4 の 32bit に 3 フィールドを収めた。**
  ★**もし queue 側が 0 でも sem 側が非0 でありうる＝この穴は残る**（**次ラウンドの候補**）。
- ★**200ms «未満» の滞留は取りこぼす**（§4.3 の設計上の限界。**①の 30 秒スケールには十分**）。
- ★**滞留の «中身»（SMP PDU だったか）は測っていない**（**種別を持たない**＝レビュー自身の限界申告と同じ）。

---

## 9. ★★★ E1 実測 ＝ **C5 × Android**（ユーザー実施・真cold・計装ビルド `e1_c5_pend`）

### 9.1 ユーザー観測（★逐語・★私の解釈と «別々に» 記録する）

> 「**Android : CONNECT -> ペアリング要求がポップアップ -> エラー**」
> 「**ペアリングポップアップがでた直後にエラーがポップアップした**」

### 9.2 マーカー実測（★全 10 本）

| reg | 値 | 意味 |
|---|---|---|
| STORE0 | `0x5ade51c0` | SYNC 到達 |
| STORE2 | `0xad000000` | adv rc=0 |
| STORE3 | `0x00000000` | `ble_hs` reset 無し |
| ★**STORE4** | ★**`0xe1000000`** | ★**tag=0xE1（サンプラ生存）／dwell_hw=**0**／pend_used=**0**** |
| STORE5 | `0x00000000` | WRITE(`0xABF3`) 未到達 |
| ★**STORE6** | ★**`0x5de00007`** | **ENC_CHANGE status=7＝`BLE_HS_ENOTCONN`** |
| STORE7 | `0xa1020704` | bt_shim 割込みトレース（予約） |
| STORE8 | `0x604e0002` | CONNECT status=**0x00**・count=**2** |
| STORE9 | `0xd15c1302` | DISCONNECT reason=**0x13**・count=**2** |

★**エンコードは «ソース» で検算した**（memory ではなく）：
`0x604E0000|(status<<8)|count`（`ble_host_smoke_c5.c:630`）／`0xD15C0000|(reason<<8)|count`（`:690`）。

### 9.3 ★★freshness（★これを «先に» 確認した＝残留を新観測と誤読しない）

| # | 検定 | 結果 |
|---|---|---|
| ★**(1)** | **前ブート（私の C0 run2）と «違う» か** | ★**STORE6 `0x5de00000`→`0x5de00007`／STORE8 `…0001`→`…0002`／STORE9 `…1301`→`…1302`＝3 本とも変化**＝**別セッション** |
| ★**(2)** | **POR は LP_AON を消すか** | ★**§8.4 の sentinel で実測済**（`0xDEADBEEF`→`0x00000000`） |
| ★**(3)** | **`used`/`hw` は前ブートの値でありうるか** | ★**不可能**＝`.bss`（毎ブート 0 初期化・§8.4 c2） |

★★**自分に第9再発を適用する（重要）**：**STORE4 は C0 の値と «バイト一致»（`0xe1000000`）だが、
この一致は «情報をほとんど運ばない»**——**私のエンコードは «何も発動しなければ ちょうど 1 つの値しか取らない»
（tag は無条件・hw=0・used=0）。⇒ 一致は «裏付け» にも «反証» にもならない。**
**freshness を保証したのは «一致» ではなく (1)(2)(3)＝操作履歴とコードの初期化挙動である。**

### 9.4 ★★失敗側の C0（＝計装が病態を変えていないか）＝ **PASS**

| 指標 | 既知ベースライン（`evidence-c5-08 §8.1`＝他エージェント・**読むだけ**） | ★**本セル（計装込み）** | 判定 |
|---|---|---|---|
| **ENC** | **`0x5de00007`**（status=7） | ★**`0x5de00007`** | ★**一致** |
| **DISC reason** | **`0x13`** | ★**`0x13`** | ★**一致** |
| **CONNECT status** | `0x00` | ★`0x00` | ★**一致** |
| **WRITE** | `0x00000000`（未到達） | ★`0x00000000` | ★**一致** |
| CONN/DISC **count** | 1 / 1 | **2 / 2** | **差**＝**セッション依存**（ユーザーが 2 回試みた）＝**署名ではない** |

⇒ ★**計装を入れても病態は «同一»**＝**計器は被験体を変えていない**（**失敗側の C0 が立った**）。
⇒ ★**よって E1 の測定は «有効»。**

★**ただし「値の一致だけで病態を同定するな」**（`evidence-stock-01 §10.3`＝**`ENC status=7` は
stock の «all ok» セッション内にも出る＝バグの指紋ではない**）。
**本セルを «失敗» と判定する根拠は «値» ではなく：ユーザーの «エラー・登録されない» ＋ WRITE=0（未到達）＋
DISC reason=0x13 が 2 回**——**この 3 つ。**

### 9.5 ★★★ E1 の一次判定（★`d38a8f3` で事前登録した分岐を «そのまま» 適用する）

**`STORE4 = 0xE1_00_0000` ⇒ `hw==0` かつ `used==0`。**

★**事前登録した分岐（§5.3・書き換えていない）**：
> **`hw==0 かつ used==0`** ⇒ ★**①は «機構ごと» 土俵に乗らない**（**E_CTX フォールバックが発動していない**）
> ⇒ ★**①は «C5×Android の説明» としては死ぬ**。ただし «滞留は無い» とは別の理由

⇒ ★★★**①（pend_ring 周期 flush の欠如）は死んだ。**
**理由は «滞留がゼロだった» ではなく «保留リングに «そもそも何も入らなかった»»。**
**flush すべきものが存在しない ⇒ «flush の欠如» は C5×Android の失敗を説明しえない。**

★**«測定無効» ではない**：**tag `0xE1` が読めた＝サンプラは生存**（`0x00000000` ではない）。
★**«計器が盲» でもない**：**§7.1 の (1)(2)(3)**——
**(1) アクセサは増分側と同一番地（`40800870`/`4080086c`）を読む・(2) E_CTX 経路はリンク済で `jal` 2 箇所・
(3) NimBLE `npl_freertos_eventq_put` が実際に shim を呼ぶ＝機構は BLE 経路上に在る。**

### 9.6 ★★独立の第2証拠：**病態の «型» が①の予測と食い違う**（★ソースで検定した）

★**①が予測する終わり方**＝**SMP PDU が滞留 → SM が対向 PDU を待つ → «我々の» タイマが焼き切れる**
⇒ ★**`BLE_HS_ETIMEOUT`(13)**（**C3 の GCC15 失敗＝`0x5de0000d` がまさにこれ**）。

★**実測＝`ENOTCONN`(7)。★そこで «7 が何を意味するか» をソースで特定した**（定数定義だけでは足りない）：

| status | 発生源（ソース） | 発火の仕方 |
|---|---|---|
| ★**`ENOTCONN`(7)** | ★**`ble_sm_connection_broken()`**（`ble_sm.c:3231-3239`）＝`res.app_status = BLE_HS_ENOTCONN; res.enc_cb = 1;`<br>**呼出し元＝`ble_gap.c:1449`・`ble_gap.c:1822`・`ble_hs_hci_evt.c:528`（HCI Disconnection 処理）** | ★**«SM proc が進行中にリンクが切れた» ときに «切断処理から同期的に» 発火**＝**待たない** |
| **`ETIMEOUT`(13)** | `ble_gap_enc_event(proc->conn_handle, BLE_HS_ETIMEOUT, 0, 0)`（`ble_sm.c:2902`）＝`ble_sm_extract_expired()` が満了 proc を回収した後 | ★**SM タイマ満了で発火** |
| ★**「30秒」の正体** | ★★**`#define BLE_SM_TIMEOUT_MS (30000)`（`ble_sm.c:67`）**＝`:441` で proc に設定 | ★**NimBLE の «定数»**＝**pend_ring の経験的性質ではない**（§3 の訂正を独立に裏付け） |

⇒ ★**`ENOTCONN` は «対向が先に降りて、SM proc が切断で殺された»**＝**«我々が 30 秒待った» ではない。**
⇒ ★**ユーザーの «ポップアップ直後にエラー»（＝«即»）と整合**：**«待ち» の署名が無い。**

★★**ただし «ENOTCONN だから①は死ぬ» とは «単独では» 書けない**（★レビュー自身が正しく警告している）：
> 「ENOTCONN(7) と ETIMEOUT(13) の違いは «どちらが先に諦めたか» で説明がつく
> （対向が先に切れば ENOTCONN、我々の 30s タイマが先なら ETIMEOUT）＝**同一機構の2つの出方でありうる**」

**＝滞留が起きて «対向が先に» 諦めれば ENOTCONN になりうる**⇒ **`ENOTCONN` 単独では①を殺せない。**
⇒ ★**だから一次判定は `STORE4` に置いた**（**used==0＝滞留の «原料» が存在しない**）。
⇒ ★**`ENOTCONN`＋«即» は «独立の第2証拠»**：**2 つの証拠が «同じ結論» を指す。**

### 9.7 ★「即」の «計器としての» 限界（★体感を秒数として記録しない）

- ★**«即» はユーザーの体感であって計器ではない。** **私は «0.5秒» 等の数値を記録しない。**
- ★**デバイス側で «CONNECT→ENC_CHANGE の実時間» を測れるか＝«測れなかった»**：
  **本ビルドの STORE6 は `tag|status` のみで «時間フィールドを持たない»**
  （**C3 は `0x5de01e0d` の byte1=`0x1e`=30 に delta を載せていた**＝**C5 の共用 STORE6 にはその余地が無い**）。
  ⇒ ★**測るには «新しいビルド＋新しいセル»＝別ラウンド。本ラウンドでは «測っていない» と明記する。**
- ★**したがって «即» は «補強» であって «一次判定» ではない**（§9.5 が一次判定）。

### 9.8 ★★事前登録した予測の全記録（★**一つも書き換えていない**）

| # | 予測 | 登録値（`d38a8f3`） | 結果 |
|---|---|---|---|
| ★**P_E1** | **滞留が実在する（dwell hw > 0）** | ★**15%** | ★★**的中**（**hw=0**＝85% 側が起きた） |
| **P_USED** | **保留経路がそもそも使われる（`pend_used > 0`）** | **70%** | ★★**外れ**（**used=0**）。★**私の «下げなかった理由» が誤っていた**＝§9.9 |
| **P_C0** | 計装込みで健全対照が bond する | 85% | ★**的中**（BlueZ 2/2・`0xABF4=BT4-OK`） |
| **P_ALIVE** | タグ `0xE1` が読める | 90% | ★**的中** |

### 9.9 ★P_USED を外した意味（★自分の誤りを記録する）

★**私は「保留経路は 70% で使われる」と予測して外した。** **なぜ外したか**：
**私は「BT コントローラはクリティカルセクション内から eventq_put を呼ぶ«はず»」と «推定» した**
（**C3 の D-2c 記録＝「BTクリティカルセクション内では tsnd_dtq が E_CTX」を読んだため**）。
**実測＝この構成では «一度も» 起きない。**
⇒ ★**pend_ring は «最後の手段»**（`psnd_dtq` «すら» E_CTX を返す文脈でのみ発動＝§7.2）
**であり、C5 の BLE 経路では «成功セルでも失敗セルでも» 発動しない＝実質デッドコード。**
⇒ ★**「機構が在る」と「機構が働く」は別**（**第10再発「escape hatch が在る ≠ 効く」と同型**）。

---

## 10. ★★★ E1 の結論と «射程»

### 10.1 結論

★★**①（「C5 app に pend_ring の周期 flush が無いから SMP PDU が滞留する」）は死んだ。**

**根拠は 2 本の «独立した» 証拠が同じ結論を指すこと**：

| # | 証拠 | 種別 |
|---|---|---|
| ★**1（一次判定）** | ★**`pend_used == 0`**＝**保留リングに «そもそも何も入っていない»**⇒**flush すべきものが存在しない**⇒**«flush の欠如» は失敗を説明しえない**。**計器の非盲は 3 通りで証明**（同一番地・リンク済で到達可能・NimBLE 経路上） | ★**実機計測（事前登録した分岐）** |
| ★**2（独立の裏付け）** | ★**病態の型が①の予測と食い違う**：①は `ETIMEOUT`(13)（**30秒＝`BLE_SM_TIMEOUT_MS` の定数**）を予測するが、実測は `ENOTCONN`(7)＝**`ble_sm_connection_broken()`＝切断でSM proc が殺された**＝**待ちの署名が無い**。**ユーザーの «ポップアップ直後» と整合** | **ソース検定＋ユーザー観測**（★**単独では①を殺せない**＝§9.6） |

★**«修正（E2）を書く «前» に» 殺せた＝E1 の設計どおりの成果。**
★**しかも E2 は C3 で «既に 1 回走って反証されていた»**（§3.2）。

### 10.2 ★射程（★これを超えて書かない）

★**①の生死は «C5 × Android が bond に到達しない» に «限る»。**
★**外挿しない 2 軸**（`evidence-stock-01 §22.3`＝**別の病態**と明記）：
- **C3 × Android＝bond するが «再接続» できない**
- **C3 × iPhone＝wedge**
★**1 つの fix が 3 つを直すと «仮定しない»。**

★**«次は②» とは書かない**——**②（C5 blob の IRK 4 関数欠如）は «既に死んでいる»**
（`evidence-stock-01 §15.4`＝**D-7 が «4 関数とも不在の C5 blob» で IRK 経路を通し、
RPA 解決＋自動再接続を成立させた**）。

### 10.3 ★★背景の訂正（★消さない＝本ラウンドのもう一つの成果）

| 訂正 | 内容 |
|---|---|
| ★**レビューの «機構は C3 で実測確定»** | ★**出典の誤読**。一次情報（`docs/bt-shim.md:2394-2410`）は **「100ms 周期 flush でも bond は成立せず→pend_ring 滞留説は反証」「shim（pend_ring/flush）は無罪」** と明記 |
| ★**«flush 有 → OK» の 3 セル** | ★**flush が «効いている» 証拠を 1 つも持たない**：**C3 の 2 セル＝flush は bond を直さなかったと実測済（「防御的安全網として残置」）／C6＝app コメント自身が「リスクへの保険」＝予防的** |
| ★**C3 の «30 秒事件» の真因** | ★**`BLE_HS_PVCY=0`**（`docs/bt-shim.md:2646`）＝**Identity 鍵配布がコンパイルアウト**。★**現在は修正済**（**実測：C5/C6 とも `CONFIG_BT_NIMBLE_HS_PVCY 1`・ELF に `ble_hs_pvcy_*` 9/9**）＝**C5 には存在しない** |
| ★**「30 秒」という数字** | ★**`#define BLE_SM_TIMEOUT_MS (30000)`（`ble_sm.c:67`）＝NimBLE の定数**。**「SM が正当に 30 秒待った」ことの証拠であって «pend_ring に滞留した» ことの証拠ではない** |
| ★★**親（コーディネータ）の誤り** | ★**「補正表は①と完全に整合＝確度は 35% より «高い» はず」は «相関を因果と読んだ» 誤り**。**表の «整合» は «残置された保険» の分布であって、flush が働いている証拠ではなかった**（**親自身がこの訂正の記録を指示した**） |
| ★**私の誤り** | ★**P_USED=70% を外した**（§9.9）＝**「機構が在る」から「使われる」を推定した**。**実測は «実質デッドコード»** |

### 10.4 ★次に測るべきもの（★**すべて «仮説»**。★犯人を名指ししない）

★**強力な軸が 1 つある**（`evidence-stock-01 §20-22`＝**読むだけ**）：
**stock ESP-IDF bleprph は «同一個体・同一 blob・同一 submodule・同一 toolchain・同一 SM config» で 3 軸とも通る**
⇒ ★**原因は «stock と我々の差» の «どこか»＝差は有限の 4 面に絞られている**（**手当たり次第ではない**）。

| 面 | 状況 | ★次の一手（**仮説**） | 私の見立て |
|---|---|---|---|
| ★**shim/OSAL** | ★**本ラウンドで «pend_ring 経路» だけは «白» になった**（`used=0`＝発動しない） | ★**shim の «他の» 経路**＝**`shim_sem_pend_total`（セマフォ側の保留）**・**`esp_shim_queue_send` の «成功経路» の遅延/取りこぼし**・**`npl` の mutex/eventq セマンティクス**。★**stock はこの層が «存在しない»（FreeRTOS 本体）** | ★**最有力**（**stock との «構造的» 差が最大**） |
| **起動**（Direct Boot・クロック） | 未計測 | 未設計 | 低（**adv/接続は到達している**） |
| **app** | ★**pend flush の欠如は «無罪» と判明**（本ラウンド） | 残る app 差は D2-D9＝**観測系**（レビューが「説明できない」と分類） | 低 |
| **config 残差 19/414** | 測定済（`evidence-stock-01 §15.6`） | ★**うち SM/privacy/store 系は `BLE_STORE_MAX_EADS` の 1 個のみ**⇒**「config 差で SM が壊れている」筋はほぼ死んでいる** | 低 |

★**具体的な «次の 1 手» の提案（＝ユーザー判断）**：
1. ★★**病態の «向き» を測る**——**現状 «なぜ対向が即座に降りるか» が «未計装»。**
   **`ENOTCONN` は «リンクが切れた» までしか言わない。**
   ⇒ ★**既存の `ESP32C5_BT_RXTRACE`（既定OFF・`ble_sm_tx`/`ble_mqueue_put`/`ble_transport_to_ll_acl_impl` を `--wrap`）が
   «我々が SMP PDU を送ったか / 対向のを受けたか» を答える**——**C3 で同じ問いに答えた実績のある計器。**
   ★**`--wrap` を使うので «噛んだかを逆アセンブルで確認» が必須**（`_impl` が正＝実測済）。
   ★**これは «滞留» ではなく «SMP の往復そのもの» を見る**＝**①の次に自然な問い。**
2. **上を «stock と我々» で対照できればなお良い**（**stock は同じ端末で通る**＝**差が出る場所が原因を含む**）。

★**私はこれを «仮説» として書く**——**本日の実測ではない。実施はユーザー判断。**

### 10.5 ★未測定・取らなかった対照（no silent caps・最終形）

- ★★**`shim_sem_pend_total`（セマフォ側の保留）＝ «測っていない»**。**STORE4 の 32bit に 3 フィールドを収めた結果、
  queue 側のみになった。** ★**①の主張は `esp_shim_queue_flush_pending`（キュー側）だが、
  C3/C6 の app は `esp_shim_sem_flush_pending()` も «同時に» 呼んでいる**
  ⇒ ★**「sem 側の保留が滞留している」可能性は «本ラウンドでは閉じていない»＝残る穴。**
  ★**ただし機構は同型（`psnd_dtq` すら撃てない文脈でのみ発動）なので、queue 側が 0 なら sem 側も 0 の «見込み» は高い——が «測っていない»。**
- ★**dwell < 200ms は見えない**（設計上の限界・§4.3）。**①の 30 秒スケールには十分だが、**
  **«短い滞留が別の悪さをする» 仮説は本計器では棄却できない。**
- ★**dwell の «中身»（SMP PDU だったか）は未測定**（**種別を持たない**）。**used=0 なので今回は «中身» 以前の話。**
- ★**iPhone C0 ＝ 未実施**（**BlueZ で C0 が立ったため。親の「BlueZ で足りるならスマホを使うな」に従った**）。
  ★**限界＝BlueZ は RPA を使わないセントラル**⇒**「RPA セントラルでも計装が無害」は §9.4 の
  «失敗側 C0»（ベースライン一致）で補ったが、«iPhone での健全 C0» は取っていない。**
- ★**「CONNECT→ENC_CHANGE の実時間」＝測っていない**（§9.7＝**マーカーに時間フィールドが無い**）。
  ★**«即» はユーザー体感であって計器ではない。**
- ★**Android セルは 1 run**（**CONN count=2＝ユーザーは 2 回試みたが、真cold セッションとしては 1**）。
  ★**ただし «既知ベースラインと同一の署名» が 3/3 の歴史（`evidence-c5-08`）と一致**＝**病態自体は再現性が高い。**
  ★**«滞留 0» の再現は 3 セッション（C0 run1・C0 run2・E1）で一貫**（**すべて `used=0`**）。
- ★**我々の ASP3 側の «他チップ» は再測していない**（**C3/C6 は電源断＝1セル1ボード**）。
